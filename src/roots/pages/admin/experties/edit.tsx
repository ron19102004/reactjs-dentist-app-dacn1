import React, { FC } from "react";
import useExpertise from "../../../../hooks/useExpertise.hook";

const EditExpertisePage: FC = () => {
  const { updateExpertise } = useExpertise();
  const submit = async () => {
    
  };
  return <div></div>;
};

export default EditExpertisePage;
